# PoC 完了条件チェックリスト（照合結果）

作成日: 2026-09-09
対象: [poc-implementation-plan.md](poc-implementation-plan.md) 第21節「PoC完了条件チェックリスト」17 項目・
第23節「今回絶対に実装しないもの」8 項目と、実装・テストの照合結果。

- テスト ID は `uv run pytest` で収集した実在のテスト関数。全 584 テストが 2026-09-09 時点で通過している。
- コードの場所はリポジトリルートからの相対パス。
- クリーンな clone からの README 再現確認は本ファイル末尾の「再現確認」を参照。

## 第21節 チェックリスト 17 項目の照合

| # | チェック項目（計画書 第21節） | 根拠（テスト / コード） |
|---|---|---|
| 1 | mock-USDC／手数料0／Root→Child一段／単一ローカルDBに限定される。 | `src/ojp/domain.py`（`ASSET_MOCK_USDC = "mock-USDC"`、`SubcontractPolicy.max_depth: 0..1`、`Bucket`・`parse_amount_units` の 6 桁小数固定）。手数料計算は `src/ojp/` 全体に存在しない（`grep -rniE "fee\|手数料" src/ojp` が 0 件）。DB は `src/ojp/scheduler.py` の `resolve_db_path`（`data/ojp.sqlite3` 1 ファイル）。単一資産・一段・手数料 0 は全 E2E（`tests/e2e/test_e01_*` 〜 `test_sequence.py`）の金額検証の前提 |
| 2 | 未入金Jobは公開されず、Claimでは送金しない。 | `tests/e2e/test_x01_unfunded_and_double_fund.py::test_x01_unfunded_root_is_not_claimable_and_funds_exactly_once`（DRAFT は Claim 拒否・二重 fund 拒否）。`tests/unit/test_jobs.py::test_fund_root_transitions_draft_to_open`（fund で DRAFT→OPEN） |
| 3 | Aの自己資金・都度承認なしにChildを発注できる。 | `tests/e2e/test_e01_child_ok_parent_ok.py::test_e01_child_ok_parent_ok`（A が MCP `ojp_create_child_job` で予算内 Child を自己資金・追加承認なしに発注）。`tests/unit/test_create_child.py::test_create_child_derives_from_catalog_and_locks_budget`。上限内の連続発注が Requester の事前許可（policy）だけで足りることは `tests/e2e/test_sequence.py`（固定 seed 操作列）でも再現 |
| 4 | 支払い済み＋拘束中の再委託上限、金額・比率・件数・深さ制限を同時実行でも守る。 | `tests/e2e/test_n07_child_creation_limits.py::test_n07_concurrent_create_child_max_amount` / `test_n07_concurrent_create_child_max_ratio` / `test_n07_concurrent_create_child_max_children`（独立プロセス同時実行）。`tests/integration/test_create_child_race.py`（同 3 ケース）。`tests/unit/test_create_child.py::test_x02_pending_child_payout_counts_toward_limit`（child_payout を U に算入）・`test_paid_child_counts_toward_u`・`test_max_children_counts_cumulative_creations`。`tests/e2e/test_x05_child_count_and_depth_limits.py::test_x05_fourth_child_hits_max_children` / `test_x05_grandchild_request_is_rejected_by_max_depth`。`tests/unit/test_create_child.py::test_create_child_from_child_is_max_depth_before_catalog` |
| 5 | Workerが条件・テスト・入力・受取人をすり替えられず、ClaimしたVersionに束縛される。 | `tests/e2e/test_x06_no_rewrite_of_conditions_or_payee.py::test_x06_conditions_verifier_input_and_payee_cannot_be_rewritten`。`tests/unit/test_create_child.py::test_create_child_rejects_condition_override_arguments`（input/expected/verifier/payee 引数は TypeError）・`test_create_child_derives_conditions_from_catalog`。`tests/unit/test_submit.py::test_submit_rejects_condition_override_arguments`・`test_submit_rejects_old_version`。`tests/unit/test_jobs.py::test_claim_rejects_wrong_version`。`src/ojp/migrations/004_job_versions_immutable.sql`（公開 Version の UPDATE/DELETE を DB トリガーで拒否。`tests/integration/test_job_lifecycle.py::test_job_versions_update_is_rejected_by_trigger` / `test_job_versions_delete_is_rejected_by_trigger`） |
| 6 | 提出前Lease失効と提出後検収タイマーが分離される。 | `tests/e2e/test_n08_submitted_survives_lease_expiry.py::test_n08_submitted_survives_old_lease_expiry`。`tests/e2e/test_x09_deadline_boundaries.py::test_x09_lease_expiry_boundary`・`test_x09_review_deadline_boundary_for_dispute` / `test_x09_review_deadline_boundary_for_approve`。`tests/integration/test_submit_expiry_race.py::test_expiry_after_valid_submit_does_nothing`・`test_submit_rejected_after_expiry_committed`。`tests/integration/test_tick.py::test_tick_keeps_submitted_between_lease_expiry_and_review_due` |
| 7 | 有効提出後の一方的な失敗／即返金を拒否する。 | `tests/e2e/test_n04_rejected_operations.py::test_n04_rejections_after_child_submission` / `test_n04_rejections_after_root_submission`。`tests/unit/test_post_submission_protection.py::test_n04_six_rejected_calls_keep_state_unchanged`・`test_n04_root_submission_rejects_refund_and_abandon`（拒否後の全表不変）。`tests/unit/test_submit.py::test_submit_pass_closes_lease_against_heartbeat`（提出で Lease を閉じる） |
| 8 | Parent FAILED/EXPIREDでも既存Childの資金・進行・成果物取得権が残る。 | `tests/e2e/test_n03_parent_fail_preserves_child.py::test_n03_parent_fail_with_leased_child` / `test_n03_parent_fail_with_submitted_child` / `test_n03_parent_fail_with_disputed_child`（Child は LEASED/SUBMITTED/DISPUTED のいずれでも存続し完遂する）。`tests/e2e/test_n09_worker_pays_after_parent_stop.py::test_n09_worker_paid_after_parent_stop_and_expiry`。`tests/e2e/test_x11_artifact_access_scope.py::test_x11_requester_reads_artifact_after_agent_stops`。`tests/integration/test_service_query.py::test_get_job_parent_independent_artifact_permission`。`tests/integration/test_tick.py::test_child_approval_survives_parent_expiry` |
| 9 | A停止後のChild処理と、検収者／裁定処理の無応答fallbackが実動する。 | `tests/e2e/test_n11_requester_unresponsive.py::test_n11_no_dispute_auto_approves_after_review_due`・`test_n11_dispute_resolved_by_fixed_arbitration`・`test_n11_unresponsive_arbiter_falls_back_to_stored_pass`。`tests/e2e/test_x14_arbitration_outcomes.py::test_x14_unresponsive_arbiter_uses_stored_pass`。`tests/unit/test_arbitration.py::test_unresponsive_arbiter_after_due_adopts_stored_pass`・`test_unresponsive_arbiter_boundary_at_due`・`test_unresponsive_arbiter_fallback_is_always_stored_pass`。A 停止後の Child 処理は N09（上段）と `tests/e2e/test_x10_restart_processes_overdue_work.py::test_x10_restart_resumes_overdue_work_without_double_effects`（全プロセス停止→再起動で過期限分を処理） |
| 10 | Child失敗はParent内部返却、Parent終了後は正確な追加返金になる。 | `tests/e2e/test_n01_child_fail_returns_child_work.py::test_n01_child_abandon_returns_child_work` / `test_n01_child_expiry_returns_child_work`。`tests/e2e/test_n13_child_return_after_parent_refund.py::test_n13_child_failure_adds_only_the_remaining_ten`・`test_n13_child_success_keeps_refund_at_ninety`。`tests/e2e/test_x12_prior_refund_protects_live_child.py`・`tests/e2e/test_x08_concurrent_refunds_and_terminal_race.py::test_x08_concurrent_refunds_converge_in_both_orders`（順序・同時でも収束）。`tests/unit/test_child_return_refund.py::test_n13_route_abandon_single_additional_refund` ほか 3 経路。`tests/integration/test_ledger.py::test_child_failure_after_parent_terminal_refund`（業務キーで 1 回）・`test_additional_refund_is_single_by_business_key` |
| 11 | Jobの検収と送金結果が独立し、送金失敗でDONEが戻らない。 | `tests/e2e/test_x03_parent_succeeds_while_child_payment_stopped.py::test_x03_parent_succeeds_while_child_payment_is_stopped`。`tests/e2e/test_n12_payment_failure_recovery.py::test_n12_payment_failures_settle_exactly_once`。`tests/unit/test_approve.py::test_payment_failure_keeps_done`（送金失敗でも DONE 維持）・`test_approve_child_moves_child_work_to_payout`（承認は予約まで。paid を増やさない）。`tests/integration/test_settlement_recovery.py::test_failpoint_before_commit_crash_leaves_nothing_and_recovers` ほか failpoint クラッシュ回復群 |
| 12 | operation_idと業務キーにより二重拘束・二重払い・二重返金を防ぐ。 | `tests/e2e/test_n05_double_approve.py::test_n05_double_approve_pays_child_worker_once`。`tests/e2e/test_x07_operation_id_reuse_and_refund_effect.py::test_x07_operation_id_reuse_is_rejected_and_refund_applies_once`。`tests/integration/test_approve_race.py::test_concurrent_approve_single_acceptance_and_payment`（同時 approve・独立プロセス）・`test_settlement_after_concurrent_approve_single_receipt`。`tests/integration/test_ledger.py::test_same_operation_id_replay_has_single_effect`・`test_same_operation_id_different_payload_is_conflict`・`test_double_fund_blocked_by_business_key_even_with_extra_seed`・`test_double_allocate_with_different_operation_ids_is_rejected`・`test_double_parent_refund_with_different_operation_ids_is_rejected`。`tests/unit/test_approve.py::test_operation_id_reuse_with_different_actor_is_idempotency_conflict` ほか冪等性契約群。`src/ojp/migrations/002_ledger_uniqueness.sql`（business_key UNIQUE） |
| 13 | 4結果と必須異常系14項目・追加テストが自動E2Eで再現される。 | 4結果: `tests/e2e/test_e01_child_ok_parent_ok.py::test_e01_child_ok_parent_ok` / `test_e02_child_fail_parent_ok.py::test_e02_child_fail_parent_ok` / `test_e03_child_ok_parent_fail.py::test_e03_child_ok_parent_fail` / `test_e04_child_fail_parent_fail.py::test_e04_child_fail_parent_fail`。異常系 14 項目: `tests/e2e/test_n01_*` 〜 `test_n14_*`（N01=Child失敗内部返却、N02=Child支払い済みParent失敗、N03=Parent失敗でもChild存続、N04=提出後の拒否操作、N05=二重承認、N06=同時Claim、N07=上限同時超過、N08=提出後Lease失効、N09=Parent停止後のChild、N10=無効な提出、N11=検収者/裁定無応答、N12=送金失敗回復、N13=先行返金後のChild、N14=各commit会計不変条件）。追加テスト: `tests/e2e/test_x01_*` 〜 `test_x15_*` と `tests/e2e/test_sequence.py::test_sequence_matches_reference_accounting_model`（固定 seed 操作列×参照会計モデル） |
| 14 | すべての確定会計操作で保存則・非負性・正しい受取人を検証する。 | `tests/e2e/test_n14_accounting_invariants.py::test_n14_invariants_hold_at_every_commit`（中間 commit を含む全 commit 直後で検査）・`test_n14_checkers_detect_violations`・`test_n14_commit_history_detects_violations_hidden_by_the_final_state`・`test_n14_every_scenario_report_enforces_the_invariants`（全シナリオがレポート検証を強制）。チェッカー実装: `tests/e2e/harness.py`（`check_conservation` / `check_payee_entitlement` / `check_no_double_counting` / `check_commit_history`）。実装側: `src/ojp/ledger.py::assert_ledger_invariants`（`check_accounts_non_negative` / `check_journal_zero_sum` / `check_conservation`）。`tests/integration/test_ledger.py::test_journal_zero_sum_per_operation_and_cumulative`。全 E2E の `conservation_ok` が最終照合 |
| 15 | MCPの8 toolsとCLI、独立tick、DB再起動復旧を実プロセスで確認する。 | MCP 8 tools: `tests/integration/test_mcp_stdio.py::test_stdio_initialize_and_tools_list`・`tests/integration/test_mcp_tools.py::test_tools_list_exact_match`・`test_all_eight_tools_in_single_session` ほか。CLI/MCP 等価性: `tests/integration/test_cli_mcp_parity.py::test_cli_and_mcp_parity`・`test_shared_db_clock_across_cli_mcp_tick`・`test_exit_code_and_mcp_error_correspondence`。CLI 全コマンド: `tests/integration/test_cli.py::test_cli_full_lifecycle_e2e` ほか。独立 tick: `tests/integration/test_tick.py::test_subprocess_tick_once_json`・`test_watch_single_iteration_equals_tick_once`。DB 再起動復旧: `tests/e2e/test_x10_restart_processes_overdue_work.py`（実プロセス停止→再起動）・`tests/integration/test_tick.py::test_overdue_processed_after_restart`・`tests/integration/test_settlement_recovery.py`（failpoint クラッシュ→新プロセス回復）・`tests/integration/test_ledger.py::test_persistence_with_fresh_connection`。E2E 全体（E01〜X15・Sequence）が CLI 子プロセス・A/B の MCP stdio・独立 tick プロセスの実プロセス構成で動く |
| 16 | READMEの手順で依存lockから起動・実行・台帳確認・テストを再現できる。 | 本ファイル末尾「再現確認」のとおり、クリーンな clone と空の DB から README の手順だけで `uv sync --locked --group dev`・`demo init`〜`ledger show`・`uv run pytest`（全 584 テスト・`data/work/e2e-reports/` へシナリオ別 JSON 生成）を確認済み |
| 17 | 除外機能を実装せず、docsと作業記録に実装済み／保留が正しく記録される。 | 除外 8 項目の未実装確認は下の表のとおり。実装状況の記録: README「現在の実装状況」（Phase 1〜8 を実装済みとして明記）と本ファイル（17 項目の根拠・除外の確認方法）。Phase 1〜7 の完了条件は計画書 第20節に対応する各 Phase のコミットで達成済み |

## 第23節 除外 8 項目の未実装確認

いずれも「未実装（意図的除外）」。確認日は 2026-09-09。`grep` は `src/`（実装コード）と
`pyproject.toml`（依存）を対象に実行した（キャッシュ `.pyc` は除外）。

| # | 除外項目（計画書 第23節） | 確認方法と結果 |
|---|---|---|
| 1 | Echtgeld／実資金、Mainnet、本番USDC、本番Escrow Smart Contract。 | 資産は `src/ojp/domain.py` の `ASSET_MOCK_USDC = "mock-USDC"` のみ。`grep -rniE "mainnet\|echtgeld" src/ pyproject.toml` → 0 件。Escrow は `src/ojp/ledger.py` の Mock 実装（`MockWallet`・In-DB `TransferReceipt`）で、外部チェーン接続のコードは無い |
| 2 | x402本番接続、独自x402 Scheme、外部Wallet／Facilitatorへの決済接続。 | `grep -rniE "x402\|facilitator" src/ pyproject.toml` → コメント 2 件のみ（`src/ojp/ledger.py` の docstring「本番 Escrow 用の chain_id・wallet 署名・x402 ヘッダー・外部 HTTP API の空実装は持たない」、`src/ojp/cli.py` の冒頭注意「本番認証・実資金・外部Facilitator連携はPoCの境界外」）。実装コード・依存は 0 件。決済は `MockWallet` 間の Journal 記帳のみ |
| 3 | 任意深度のParent/Child、複数Registry同期、高度な分散構成、マイクロサービス。 | 深さは `src/ojp/domain.py` の `SubcontractPolicy.max_depth: Field(ge=0, le=1)` で 0..1 に型レベルで制限。孫発注は `tests/unit/test_create_child.py::test_create_child_from_child_is_max_depth_before_catalog`・`tests/e2e/test_x05_*` が MAX_DEPTH 拒否を検証。Registry 同期・メッセージブローカー・マイクロサービスの要素は `grep -rniE "kafka\|rabbitmq\|redis\|celery\|microservice" src/ pyproject.toml` → 0 件。構成は計画書 第3節どおり 1 コードベース・1 SQLite ファイル |
| 4 | Reputation、Stake、ZK、Reviewer市場、Arbiter市場。 | `grep -rniE "reputation\|stake\|zero.?knowledge\|reviewer\|marketplace" src/ pyproject.toml` → 0 件。裁定は `src/ojp/verification.py` の固定決定論的検証器（`tests/unit/test_arbitration.py`）で、市場・人間の主観裁定は無い |
| 5 | 主観的成果物の判定、複雑な修正ループ、公開後の仕様変更交渉。 | 判定は `src/ojp/verification.py`（sum-v1 の固定条件照合）のみ。`CHANGES_REQUESTED` は状態機械に存在しない（`src/ojp/domain.py` の `AcceptanceDecision` は APPROVED/REJECTED のみ）。公開後の Version 変更は `src/ojp/migrations/004_job_versions_immutable.sql` のトリガーで DB レベル拒否。修正は「Lease 中の再提出」（`tests/unit/test_submit.py::test_submit_fail_then_corrected_resubmission_succeeds`）だけ |
| 6 | GitHub連携、Upwork連携、CrowdWorks連携、Web Marketplace。 | `grep -rniE "github\|upwork\|crowdworks\|webhook\|marketplace" src/ pyproject.toml` → 0 件（外部 HTTP 通信を行うコードは存在しない） |
| 7 | 本番認証基盤、公開HTTP API、任意コード実行基盤。 | `src/`（実装コード）に `grep -rniE "fastapi\|flask\|uvicorn\|http.server\|HTTPServer"` → 0 件。`pyproject.toml` の直接依存は `mcp`・`pydantic`・開発 group のみ（`uvicorn` は MCP SDK の stdio 以外の transport 用オプション依存として `uv.lock` に記録されるだけで、OJP のコードは一切使わない。README の手順で入る `ojp mcp` は stdio）。署名・鍵管理は `grep -rniE "private.?key\|signing\|ecdsa\|ed25519\|secp256k1" src/ pyproject.toml` → 0 件（`--actor` はローカル信頼境界の指定。README「認証の境界」参照）。任意コード実行は行わず、成果物は最大 1 MiB の JSON として DB 保存（`src/ojp/domain.py::MAX_ARTIFACT_BYTES`） |
| 8 | この計画作成ターンでの実装コード、依存導入、DB作成、アプリテスト実行。 | 計画作成ターン（2026-09-06）では計画書のみ作成し実装しなかった。実装は以後の別ターン（Phase 1〜8、コミット `4f1a33d` 〜 `c6c8364`）で実施。本項は「計画作成ターンでは実装しない」旨の約束であり、Phase 1〜8 の実施を妨げない。現時点の実装状況は README「現在の実装状況」と上の 17 項目の照合が記録する |

## 再現確認（クリーンなチェックアウトから）

2026-09-09 に、既存の作業ディレクトリとは別の一時ディレクトリへ本リポジトリを
`git clone` し（既存の `data/ojp.sqlite3` を持ち込まない空の DB 状態から）、
README の手順だけをなぞって確認した:

1. `uv sync --locked --group dev` — README「セットアップ」どおりに成功。
2. README「最小実行手順」1〜12（`demo init` から `ledger show` まで）— すべて到達。
3. `uv run pytest -q` — 全 584 テストが通過し、`data/work/e2e-reports/` に
   シナリオごとの JSON レポート（`E01` 〜 `E04`、`N01A` 〜 `N14`、`X01` 〜 `X15` の
   枝番、`SEQ*`）が生成されることを確認。

レポートの各フィールドの意味は README「テストレポートの読み方」を参照。
